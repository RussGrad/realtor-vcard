/**
 * Доступ в личный кабинет. Замените значения перед публикацией сайта.
 *
 * Способ 1 — простой (пароль хранится в файле в открытом виде):
 *   Задайте своё значение в password.
 *
 * Способ 2 — без открытого пароля в файле:
 *   Оставьте password пустым ("") и задайте passwordSha256Hex — hex SHA-256 от пароля.
 *   В терминале:  printf 'ваш_пароль' | shasum -a 256
 *   (без перевода строки в конце, если нужен другой способ — см. документацию openssl)
 */
window.REALTOR_CABINET_AUTH = {
  /** Пароль для входа (без лишних пробелов по краям при проверке) */
  password: "Realty2026",

  /**
   * Режим «только hash»: укажите 64 hex-символа SHA-256, иначе null.
   * null + непустой password — вход по полю password выше.
   */
  passwordSha256Hex: null,
};
